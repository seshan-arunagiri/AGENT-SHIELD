// Mock Database MCP Tool definition
// Used for demo and testing purposes only — not a real database connection.
export const databaseTool = {
  name: "database",
  description: "Query and mutate a relational database via natural language.",
  version: "1.0.0",
  endpoints: [] as string[],
};
